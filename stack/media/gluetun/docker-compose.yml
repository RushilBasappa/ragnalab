# Gluetun VPN Container - Network tunnel for torrent privacy
# All torrent traffic from qBittorrent routes through this VPN
# Access qBittorrent WebUI at: https://qbit.ragnalab.xyz (via Traefik)

services:
  gluetun:
    image: qmcgaw/gluetun:latest
    container_name: gluetun
    restart: unless-stopped
    profiles: ["media"]
    cap_add:
      - NET_ADMIN
    devices:
      - /dev/net/tun:/dev/net/tun
    environment:
      # VPN Provider configuration
      - VPN_SERVICE_PROVIDER=${VPN_SERVICE_PROVIDER}
      - VPN_TYPE=wireguard
      # WireGuard credentials
      - WIREGUARD_PRIVATE_KEY=${WIREGUARD_PRIVATE_KEY}
      - WIREGUARD_ADDRESSES=${WIREGUARD_ADDRESSES}
      # Server location preference (Netherlands supports port forwarding)
      - SERVER_COUNTRIES=${SERVER_COUNTRIES:-Netherlands}
      # Port forwarding for torrents (required for incoming connections)
      - VPN_PORT_FORWARDING=on
      - VPN_PORT_FORWARDING_PROVIDER=protonvpn
      # Write forwarded port to shared volume for port-manager sidecar
      - VPN_PORT_FORWARDING_STATUS_FILE=/gluetun/forwarded_port
      # Timezone
      - TZ=America/Los_Angeles
    volumes:
      - gluetun-data:/gluetun
    networks:
      - media
      - proxy
    # Note: Gluetun has built-in healthcheck at http://127.0.0.1:9999/
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.25'
        reservations:
          memory: 64M
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      # --- Traefik labels for qBittorrent WebUI ---
      - "traefik.enable=true"
      - "traefik.http.routers.qbittorrent.rule=Host(`qbit.ragnalab.xyz`)"
      - "traefik.http.routers.qbittorrent.entrypoints=websecure"
      - "traefik.http.routers.qbittorrent.tls=true"
      - "traefik.http.routers.qbittorrent.tls.certresolver=letsencrypt"
      - "traefik.http.services.qbittorrent.loadbalancer.server.port=8080"
      - "traefik.docker.network=proxy"
      # --- Homepage labels for qBittorrent ---
      - "homepage.group=Media"
      - "homepage.name=qBittorrent"
      - "homepage.icon=qbittorrent.png"
      - "homepage.href=https://qbit.ragnalab.xyz"
      - "homepage.description=Torrent Client"
      - "homepage.widget.type=qbittorrent"
      - "homepage.widget.url=http://gluetun:8080"
      - "homepage.widget.username=${QBIT_USERNAME}"
      - "homepage.widget.password=${QBIT_PASSWORD}"
      - "homepage.server=ragnalab-docker"
      # --- Autokuma monitoring ---
      - "kuma.gluetun-container.docker.name=Gluetun VPN"
      - "kuma.gluetun-container.docker.docker_container=gluetun"
      - "kuma.gluetun-container.docker.docker_host_name=ragnalab-docker"
      - "kuma.gluetun-container.docker.parent_name=media-group"
      - "kuma.qbittorrent.http.name=qBittorrent"
      - "kuma.qbittorrent.http.url=https://qbit.ragnalab.xyz"
      - "kuma.qbittorrent.http.parent_name=media-group"

  # Port Manager - Syncs Gluetun forwarded port to qBittorrent
  port-manager:
    image: snoringdragon/gluetun-qbittorrent-port-manager:latest
    container_name: port-manager
    restart: unless-stopped
    profiles: ["media"]
    depends_on:
      gluetun:
        condition: service_healthy
    network_mode: "container:gluetun"
    environment:
      - QBITTORRENT_SERVER=localhost
      - QBITTORRENT_PORT=8080
      - QBITTORRENT_USER=${QBIT_USERNAME:-admin}
      - QBITTORRENT_PASS=${QBIT_PASSWORD:-adminadmin}
      - PORT_FORWARDED=/tmp/gluetun/forwarded_port
      - HTTP_S=http
    volumes:
      - gluetun-data:/tmp/gluetun:ro
    deploy:
      resources:
        limits:
          memory: 64M
          cpus: '0.1'
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "2"

volumes:
  gluetun-data:
    external: true
    name: ragnalab_gluetun-data

networks:
  media:
    external: true
  proxy:
    external: true
