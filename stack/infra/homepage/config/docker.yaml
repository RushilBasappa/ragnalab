# Docker Socket Configuration via socket-proxy
# https://gethomepage.dev/configs/docker/

# Main Docker host - used for container discovery via labels
# Uses TCP connection to socket-proxy for security (no direct socket mount)
my-docker:
  host: socket-proxy
  port: 2375
