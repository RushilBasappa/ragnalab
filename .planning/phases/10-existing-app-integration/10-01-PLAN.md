---
phase: 10-existing-app-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - stack/infra/homepage/docker-compose.yml
  - stack/infra/glances/docker-compose.yml
  - stack/infra/traefik/docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "User is redirected to Authelia when accessing home.ragnalab.xyz"
    - "User is redirected to Authelia when accessing glances.ragnalab.xyz"
    - "User is redirected to Authelia when accessing traefik.ragnalab.xyz"
    - "After Authelia login, user lands on the original service"
    - "SSO session persists - second service doesn't require re-login"
  artifacts:
    - path: "stack/infra/homepage/docker-compose.yml"
      provides: "Homepage with authelia middleware"
      contains: "middlewares=authelia@file"
    - path: "stack/infra/glances/docker-compose.yml"
      provides: "Glances with authelia middleware"
      contains: "middlewares=authelia@file"
    - path: "stack/infra/traefik/docker-compose.yml"
      provides: "Traefik dashboard with authelia middleware"
      contains: "middlewares=authelia@file"
  key_links:
    - from: "Traefik router labels"
      to: "authelia@file middleware"
      via: "traefik.http.routers.*.middlewares label"
      pattern: "middlewares=authelia@file"
---

<objective>
Protect Homepage, Glances, and Traefik dashboard with Authelia SSO middleware.

Purpose: These "simple" services have no built-in authentication. Adding the ForwardAuth middleware label is all that's needed to require Authelia login. This establishes the pattern for all subsequent service integrations.

Output: Three docker-compose.yml files updated with authelia@file middleware labels. All three services require Authelia authentication to access.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-existing-app-integration/10-RESEARCH.md

# Existing middleware configuration
@stack/infra/traefik/config/dynamic/middlewares.yml

# Services to modify
@stack/infra/homepage/docker-compose.yml
@stack/infra/glances/docker-compose.yml
@stack/infra/traefik/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Authelia middleware to Homepage</name>
  <files>stack/infra/homepage/docker-compose.yml</files>
  <action>
Add the Authelia ForwardAuth middleware label to the Homepage service.

In the labels section, add this line after the existing Traefik router labels:
```yaml
- "traefik.http.routers.homepage.middlewares=authelia@file"
```

Place it after the `traefik.http.services.homepage.loadbalancer.server.port=3000` line and before the `traefik.docker.network=proxy` line for consistency.

Note: Homepage already has HOMEPAGE_ALLOWED_HOSTS=home.ragnalab.xyz set, which is correct for Authelia redirects.
  </action>
  <verify>
Run `grep -A2 "routers.homepage.tls.certresolver" stack/infra/homepage/docker-compose.yml` to confirm middleware label is present.
  </verify>
  <done>Homepage docker-compose.yml contains `traefik.http.routers.homepage.middlewares=authelia@file` label</done>
</task>

<task type="auto">
  <name>Task 2: Add Authelia middleware to Glances</name>
  <files>stack/infra/glances/docker-compose.yml</files>
  <action>
Add the Authelia ForwardAuth middleware label to the Glances service.

In the labels section, add this line after the existing Traefik router labels:
```yaml
- "traefik.http.routers.glances.middlewares=authelia@file"
```

Place it after the `traefik.http.services.glances.loadbalancer.server.port=61208` line and before the `traefik.docker.network=proxy` line.

Note: Glances runs without authentication by default (no password set), so the middleware is the only protection layer.
  </action>
  <verify>
Run `grep -A2 "routers.glances.tls.certresolver" stack/infra/glances/docker-compose.yml` to confirm middleware label is present.
  </verify>
  <done>Glances docker-compose.yml contains `traefik.http.routers.glances.middlewares=authelia@file` label</done>
</task>

<task type="auto">
  <name>Task 3: Add Authelia middleware to Traefik dashboard</name>
  <files>stack/infra/traefik/docker-compose.yml</files>
  <action>
Add the Authelia ForwardAuth middleware label to the Traefik dashboard router.

In the labels section, add this line after the existing dashboard router labels:
```yaml
- "traefik.http.routers.dashboard.middlewares=authelia@file"
```

Place it after the `traefik.http.routers.dashboard.tls.certresolver=letsencrypt` line and before the comment about security-headers.

Note: The comment says "security-headers@file omitted - CSP blocks dashboard's inline scripts". We're only adding authelia@file, not chaining with security-headers.
  </action>
  <verify>
Run `grep -A2 "routers.dashboard.tls.certresolver" stack/infra/traefik/docker-compose.yml` to confirm middleware label is present.
  </verify>
  <done>Traefik docker-compose.yml contains `traefik.http.routers.dashboard.middlewares=authelia@file` label</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. Redeploy the stack:
   ```bash
   cd /home/rushil/workspace/ragnalab && make up
   ```

2. Test Homepage SSO:
   - Open incognito browser
   - Navigate to https://home.ragnalab.xyz
   - Verify redirect to https://auth.ragnalab.xyz
   - Complete Authelia login (password + passkey)
   - Verify redirect back to Homepage

3. Test Glances SSO (same session):
   - Navigate to https://glances.ragnalab.xyz
   - Should NOT prompt for login (SSO session active)
   - Verify system stats are visible

4. Test Traefik dashboard SSO:
   - Navigate to https://traefik.ragnalab.xyz
   - Should NOT prompt for login (SSO session)
   - Verify dashboard loads (requires admin group per Authelia ACL)

5. Verify Autokuma monitors still work:
   - Check Uptime Kuma at https://status.ragnalab.xyz
   - Verify Homepage, Glances, Traefik monitors show UP
   - Note: If monitors show DOWN, they may need to be updated to use internal URLs (http://container:port) in a future task
</verification>

<success_criteria>
- [ ] Homepage requires Authelia login to access
- [ ] Glances requires Authelia login to access
- [ ] Traefik dashboard requires Authelia login to access
- [ ] SSO session persists across all three services
- [ ] All three services accessible after single Authelia login
- [ ] Changes committed to git
</success_criteria>

<output>
After completion, create `.planning/phases/10-existing-app-integration/10-01-SUMMARY.md`
</output>
