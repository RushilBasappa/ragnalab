---
phase: 07-operational-hardening
plan: 08
type: execute
wave: 4
depends_on: ["07-05", "07-07"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User verifies all services accessible via HTTPS"
    - "User verifies Homepage shows all services with status"
    - "User verifies Uptime Kuma has monitors for all services"
    - "User verifies backup system works with new structure"
  artifacts: []
  key_links: []
---

<objective>
Verify complete Phase 7 implementation with human verification checkpoint.

Purpose: Ensure all operational hardening work is functional before marking phase complete.

Output: Human-verified confirmation that compose restructuring, socket-proxy migration, backup expansion, and Autokuma integration are all working correctly.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-operational-hardening/07-05-SUMMARY.md
@.planning/phases/07-operational-hardening/07-07-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete Phase 7: Operational Hardening including:
- Docker Compose restructured into stack/ with nested includes
- Socket-proxy migration for Uptime Kuma and Homepage
- Backup system expanded with missing volumes
- Autokuma deployed with labels on all services
- Makefile simplified to operational essentials
  </what-built>
  <how-to-verify>
**1. Compose Structure Verification:**
```bash
# Verify stack/ directory structure
ls -la stack/infra/ stack/media/ stack/apps/

# Test profile commands work
docker compose --profile infra ps
docker compose --profile media ps
docker compose --profile apps ps
```

**2. Service Accessibility:**
Visit each URL and verify working:
- https://traefik.ragnalab.xyz (Traefik dashboard)
- https://status.ragnalab.xyz (Uptime Kuma)
- https://home.ragnalab.xyz (Homepage)
- https://vault.ragnalab.xyz (Vaultwarden)
- https://pihole.ragnalab.xyz (Pi-hole)
- https://glances.ragnalab.xyz (Glances)
- https://prowlarr.ragnalab.xyz (Prowlarr)
- https://sonarr.ragnalab.xyz (Sonarr)
- https://radarr.ragnalab.xyz (Radarr)
- https://bazarr.ragnalab.xyz (Bazarr)
- https://jellyfin.ragnalab.xyz (Jellyfin)
- https://requests.ragnalab.xyz (Jellyseerr)

**3. Socket-Proxy Verification:**
```bash
# Verify no containers mount docker.sock except allowed ones
docker ps --format "{{.Names}}" | while read c; do
  mounts=$(docker inspect $c --format '{{range .Mounts}}{{.Source}} {{end}}')
  if echo "$mounts" | grep -q "docker.sock"; then
    echo "$c mounts docker.sock"
  fi
done
# Expected: socket-proxy, backup, glances only
```

**4. Uptime Kuma Docker Host:**
- Go to https://status.ragnalab.xyz
- Navigate to Settings > Docker Hosts
- Verify "my-docker" host configured with socket-proxy:2375
- Test connection shows "Connected"

**5. Autokuma Monitors:**
- In Uptime Kuma, verify monitors exist for all services
- Verify monitors have "autokuma" tag
- Verify monitors grouped by: Infrastructure, Media, Apps, Containers
- Estimated total: ~34 monitors

**6. Homepage Docker Integration:**
- Visit https://home.ragnalab.xyz
- Verify container status shows for all services
- Verify widgets show real data (not errors)

**7. Backup System:**
```bash
# Trigger manual backup
make backup

# Verify new volumes in backup
tar -tzf backups/backup-latest.tar.gz | grep -E "uptime-kuma|rustdesk|traefik-acme"
```

**8. Makefile:**
```bash
# Verify only operational targets
grep "^[a-z]*:" Makefile
# Should show: backup:, restore:, status:

# Test status command
make status
```
  </how-to-verify>
  <resume-signal>Type "verified" to confirm all checks pass, or describe any issues found.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update STATE.md and commit final changes</name>
  <files>
    - .planning/STATE.md
  </files>
  <action>
After human verification passes:

1. Update STATE.md:
   - Current position: Phase 7 complete
   - Update v2.0 Scope checkboxes for Phase 7 items
   - Add key decisions from Phase 7
   - Update services deployed table if needed
   - Set next action to Phase 8

2. Update ROADMAP.md:
   - Mark Phase 7 as Complete with date
   - Update plans count (8 plans)

3. Commit all Phase 7 changes:
   ```bash
   git add -A
   git commit -m "feat(07): complete operational hardening phase

   - Restructured compose into stack/ with nested includes
   - Migrated Uptime Kuma and Homepage to socket-proxy
   - Expanded backup coverage (uptime-kuma, rustdesk, traefik-acme)
   - Deployed Autokuma for automated monitoring
   - Added kuma labels to all services (~34 monitors)
   - Simplified Makefile to operational essentials
   - Archived old proxy/ and apps/ directories

   Co-Authored-By: Claude Opus 4.5 <noreply@anthropic.com>"
   ```
  </action>
  <verify>
```bash
# Verify STATE.md updated
grep "Phase 7" .planning/STATE.md

# Verify ROADMAP.md updated
grep -A2 "Phase 7:" .planning/ROADMAP.md

# Verify commit created
git log --oneline -1
```
  </verify>
  <done>STATE.md and ROADMAP.md updated. Phase 7 marked complete. All changes committed.</done>
</task>

</tasks>

<verification>
- [ ] All HTTPS services accessible
- [ ] Homepage shows container status for all services
- [ ] Uptime Kuma has ~34 monitors with autokuma tag
- [ ] Socket-proxy used by Uptime Kuma and Homepage (verified in Docker hosts)
- [ ] Only socket-proxy, backup, glances mount docker.sock
- [ ] Backup archive contains uptime-kuma, rustdesk, traefik-acme
- [ ] Makefile has only backup, restore, status targets
- [ ] Docker compose profiles (infra, media, apps) all work
- [ ] STATE.md and ROADMAP.md updated
- [ ] Final commit created
</verification>

<success_criteria>
Phase 7: Operational Hardening is complete. User has verified all components working. Documentation updated. Ready for Phase 8: Application Expansion.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operational-hardening/07-08-SUMMARY.md`
</output>
