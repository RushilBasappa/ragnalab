---
phase: 07-operational-hardening
plan: 07
type: execute
wave: 3
depends_on: ["07-06"]
files_modified:
  - stack/media/gluetun/docker-compose.yml
  - stack/media/qbittorrent/docker-compose.yml
  - stack/media/prowlarr/docker-compose.yml
  - stack/media/sonarr/docker-compose.yml
  - stack/media/radarr/docker-compose.yml
  - stack/media/bazarr/docker-compose.yml
  - stack/media/unpackerr/docker-compose.yml
  - stack/media/jellyfin/docker-compose.yml
  - stack/media/jellyseerr/docker-compose.yml
  - stack/apps/vaultwarden/docker-compose.yml
  - stack/apps/pihole/docker-compose.yml
  - stack/apps/rustdesk/docker-compose.yml
  - stack/apps/glances/docker-compose.yml
autonomous: true

must_haves:
  truths:
    - "All media services have kuma labels for automatic monitoring"
    - "All app services have kuma labels for automatic monitoring"
    - "Autokuma creates monitors for all services when they start"
    - "Uptime Kuma shows complete monitoring for entire stack"
  artifacts:
    - path: "stack/media/jellyfin/docker-compose.yml"
      provides: "Jellyfin with kuma labels"
      contains: "kuma.jellyfin"
    - path: "stack/apps/vaultwarden/docker-compose.yml"
      provides: "Vaultwarden with kuma labels"
      contains: "kuma.vaultwarden"
  key_links:
    - from: "stack/media/*/docker-compose.yml"
      to: "Autokuma"
      via: "kuma labels"
      pattern: "kuma\\..+\\.http|docker"
---

<objective>
Add kuma labels to all media and app services for comprehensive automated monitoring.

Purpose: Complete Autokuma integration by labeling all remaining services (media stack and apps) for automatic monitor creation.

Output: All services across the entire stack have kuma labels. Uptime Kuma has complete visibility into all running services.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-operational-hardening/07-RESEARCH.md
@.planning/phases/07-operational-hardening/07-06-SUMMARY.md

# Media service compose files
@stack/media/gluetun/docker-compose.yml
@stack/media/qbittorrent/docker-compose.yml
@stack/media/prowlarr/docker-compose.yml
@stack/media/sonarr/docker-compose.yml
@stack/media/radarr/docker-compose.yml
@stack/media/bazarr/docker-compose.yml
@stack/media/unpackerr/docker-compose.yml
@stack/media/jellyfin/docker-compose.yml
@stack/media/jellyseerr/docker-compose.yml

# App service compose files
@stack/apps/vaultwarden/docker-compose.yml
@stack/apps/pihole/docker-compose.yml
@stack/apps/rustdesk/docker-compose.yml
@stack/apps/glances/docker-compose.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add kuma labels to media services</name>
  <files>
    - stack/media/gluetun/docker-compose.yml
    - stack/media/qbittorrent/docker-compose.yml
    - stack/media/prowlarr/docker-compose.yml
    - stack/media/sonarr/docker-compose.yml
    - stack/media/radarr/docker-compose.yml
    - stack/media/bazarr/docker-compose.yml
    - stack/media/unpackerr/docker-compose.yml
    - stack/media/jellyfin/docker-compose.yml
    - stack/media/jellyseerr/docker-compose.yml
  </files>
  <action>
Add kuma labels to all media services.

**Services with HTTP endpoints (HTTP + Container monitors):**

1. **Prowlarr** (stack/media/prowlarr/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.prowlarr.http.name=Prowlarr"
  - "kuma.prowlarr.http.url=https://prowlarr.ragnalab.xyz"
  - "kuma.prowlarr.http.parent_name=Media"
  - "kuma.prowlarr-container.docker.name=Prowlarr Container"
  - "kuma.prowlarr-container.docker.docker_container=prowlarr"
  - "kuma.prowlarr-container.docker.docker_host=my-docker"
  - "kuma.prowlarr-container.docker.parent_name=Containers"
```

2. **Sonarr** (stack/media/sonarr/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.sonarr.http.name=Sonarr"
  - "kuma.sonarr.http.url=https://sonarr.ragnalab.xyz"
  - "kuma.sonarr.http.parent_name=Media"
  - "kuma.sonarr-container.docker.name=Sonarr Container"
  - "kuma.sonarr-container.docker.docker_container=sonarr"
  - "kuma.sonarr-container.docker.docker_host=my-docker"
  - "kuma.sonarr-container.docker.parent_name=Containers"
```

3. **Radarr** (stack/media/radarr/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.radarr.http.name=Radarr"
  - "kuma.radarr.http.url=https://radarr.ragnalab.xyz"
  - "kuma.radarr.http.parent_name=Media"
  - "kuma.radarr-container.docker.name=Radarr Container"
  - "kuma.radarr-container.docker.docker_container=radarr"
  - "kuma.radarr-container.docker.docker_host=my-docker"
  - "kuma.radarr-container.docker.parent_name=Containers"
```

4. **Bazarr** (stack/media/bazarr/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.bazarr.http.name=Bazarr"
  - "kuma.bazarr.http.url=https://bazarr.ragnalab.xyz"
  - "kuma.bazarr.http.parent_name=Media"
  - "kuma.bazarr-container.docker.name=Bazarr Container"
  - "kuma.bazarr-container.docker.docker_container=bazarr"
  - "kuma.bazarr-container.docker.docker_host=my-docker"
  - "kuma.bazarr-container.docker.parent_name=Containers"
```

5. **Jellyfin** (stack/media/jellyfin/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.jellyfin.http.name=Jellyfin"
  - "kuma.jellyfin.http.url=https://jellyfin.ragnalab.xyz"
  - "kuma.jellyfin.http.parent_name=Media"
  - "kuma.jellyfin-container.docker.name=Jellyfin Container"
  - "kuma.jellyfin-container.docker.docker_container=jellyfin"
  - "kuma.jellyfin-container.docker.docker_host=my-docker"
  - "kuma.jellyfin-container.docker.parent_name=Containers"
```

6. **Jellyseerr** (stack/media/jellyseerr/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.jellyseerr.http.name=Jellyseerr"
  - "kuma.jellyseerr.http.url=https://requests.ragnalab.xyz"
  - "kuma.jellyseerr.http.parent_name=Media"
  - "kuma.jellyseerr-container.docker.name=Jellyseerr Container"
  - "kuma.jellyseerr-container.docker.docker_container=jellyseerr"
  - "kuma.jellyseerr-container.docker.docker_host=my-docker"
  - "kuma.jellyseerr-container.docker.parent_name=Containers"
```

**Services with container monitor only (no HTTP endpoint):**

7. **Gluetun** (stack/media/gluetun/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.gluetun.docker.name=Gluetun VPN"
  - "kuma.gluetun.docker.docker_container=gluetun"
  - "kuma.gluetun.docker.docker_host=my-docker"
  - "kuma.gluetun.docker.parent_name=Containers"
```

8. **qBittorrent** (stack/media/qbittorrent/docker-compose.yml):
Note: qBittorrent may not have labels block if using network_mode. Add labels section if missing.
```yaml
labels:
  # ... existing labels ...
  - "kuma.qbittorrent.docker.name=qBittorrent"
  - "kuma.qbittorrent.docker.docker_container=qbittorrent"
  - "kuma.qbittorrent.docker.docker_host=my-docker"
  - "kuma.qbittorrent.docker.parent_name=Containers"
```

9. **Unpackerr** (stack/media/unpackerr/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.unpackerr.docker.name=Unpackerr"
  - "kuma.unpackerr.docker.docker_container=unpackerr"
  - "kuma.unpackerr.docker.docker_host=my-docker"
  - "kuma.unpackerr.docker.parent_name=Containers"
```
  </action>
  <verify>
```bash
# Verify kuma labels added to media services
for svc in gluetun qbittorrent prowlarr sonarr radarr bazarr unpackerr jellyfin jellyseerr; do
  count=$(grep -c "kuma\." stack/media/$svc/docker-compose.yml 2>/dev/null || echo 0)
  echo "$svc: $count labels"
done

# Restart media services to apply labels
docker compose --profile media up -d

# Check Autokuma detects new services
sleep 30
docker logs autokuma 2>&1 | grep -E "Creating|created" | tail -15
```
  </verify>
  <done>All 9 media services have kuma labels. HTTP services have both HTTP and container monitors. Headless services have container monitors only.</done>
</task>

<task type="auto">
  <name>Task 2: Add kuma labels to app services</name>
  <files>
    - stack/apps/vaultwarden/docker-compose.yml
    - stack/apps/pihole/docker-compose.yml
    - stack/apps/rustdesk/docker-compose.yml
    - stack/apps/glances/docker-compose.yml
  </files>
  <action>
Add kuma labels to all app services.

1. **Vaultwarden** (stack/apps/vaultwarden/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.vaultwarden.http.name=Vaultwarden"
  - "kuma.vaultwarden.http.url=https://vault.ragnalab.xyz"
  - "kuma.vaultwarden.http.parent_name=Apps"
  - "kuma.vaultwarden-container.docker.name=Vaultwarden Container"
  - "kuma.vaultwarden-container.docker.docker_container=vaultwarden"
  - "kuma.vaultwarden-container.docker.docker_host=my-docker"
  - "kuma.vaultwarden-container.docker.parent_name=Containers"
```

2. **Pi-hole** (stack/apps/pihole/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.pihole.http.name=Pi-hole"
  - "kuma.pihole.http.url=https://pihole.ragnalab.xyz"
  - "kuma.pihole.http.parent_name=Apps"
  - "kuma.pihole-container.docker.name=Pi-hole Container"
  - "kuma.pihole-container.docker.docker_container=pihole"
  - "kuma.pihole-container.docker.docker_host=my-docker"
  - "kuma.pihole-container.docker.parent_name=Containers"
```

3. **RustDesk** (stack/apps/rustdesk/docker-compose.yml):
RustDesk has two containers (hbbs, hbbr) and uses host networking. Add TCP port monitors:
```yaml
# For hbbs service:
labels:
  # ... existing labels ...
  - "kuma.rustdesk-hbbs.tcp.name=RustDesk ID Server"
  - "kuma.rustdesk-hbbs.tcp.hostname=100.75.173.7"
  - "kuma.rustdesk-hbbs.tcp.port=21116"
  - "kuma.rustdesk-hbbs.tcp.parent_name=Apps"
  - "kuma.rustdesk-hbbs-container.docker.name=RustDesk HBBS Container"
  - "kuma.rustdesk-hbbs-container.docker.docker_container=rustdesk-hbbs"
  - "kuma.rustdesk-hbbs-container.docker.docker_host=my-docker"
  - "kuma.rustdesk-hbbs-container.docker.parent_name=Containers"

# For hbbr service:
labels:
  - "kuma.rustdesk-hbbr.tcp.name=RustDesk Relay Server"
  - "kuma.rustdesk-hbbr.tcp.hostname=100.75.173.7"
  - "kuma.rustdesk-hbbr.tcp.port=21117"
  - "kuma.rustdesk-hbbr.tcp.parent_name=Apps"
  - "kuma.rustdesk-hbbr-container.docker.name=RustDesk HBBR Container"
  - "kuma.rustdesk-hbbr-container.docker.docker_container=rustdesk-hbbr"
  - "kuma.rustdesk-hbbr-container.docker.docker_host=my-docker"
  - "kuma.rustdesk-hbbr-container.docker.parent_name=Containers"
```

4. **Glances** (stack/apps/glances/docker-compose.yml):
```yaml
labels:
  # ... existing labels ...
  - "kuma.glances.http.name=Glances"
  - "kuma.glances.http.url=https://glances.ragnalab.xyz"
  - "kuma.glances.http.parent_name=Apps"
  - "kuma.glances-container.docker.name=Glances Container"
  - "kuma.glances-container.docker.docker_container=glances"
  - "kuma.glances-container.docker.docker_host=my-docker"
  - "kuma.glances-container.docker.parent_name=Containers"
```

Restart app services to apply labels:
```bash
docker compose --profile apps up -d
```
  </action>
  <verify>
```bash
# Verify kuma labels added to app services
for svc in vaultwarden pihole rustdesk glances; do
  count=$(grep -c "kuma\." stack/apps/$svc/docker-compose.yml 2>/dev/null || echo 0)
  echo "$svc: $count labels"
done

# Restart app services
docker compose --profile apps up -d

# Check Autokuma detects new services
sleep 30
docker logs autokuma 2>&1 | grep -E "Creating|created" | tail -10
```
  </verify>
  <done>All 4 app services have kuma labels. Vaultwarden, Pi-hole, Glances have HTTP + container monitors. RustDesk has TCP + container monitors for both hbbs and hbbr.</done>
</task>

<task type="auto">
  <name>Task 3: Verify complete monitoring coverage</name>
  <files>None (verification only)</files>
  <action>
Verify Autokuma has created monitors for all services:

1. Check total monitor count expectation:
   - Infrastructure HTTP: 3 (Traefik, Uptime Kuma, Homepage)
   - Infrastructure Container: 6 (Traefik, Uptime Kuma, Homepage, Socket-proxy, Backup, Autokuma)
   - Media HTTP: 6 (Prowlarr, Sonarr, Radarr, Bazarr, Jellyfin, Jellyseerr)
   - Media Container: 9 (all media services)
   - Apps HTTP: 3 (Vaultwarden, Pi-hole, Glances)
   - Apps TCP: 2 (RustDesk hbbs, hbbr)
   - Apps Container: 5 (Vaultwarden, Pi-hole, RustDesk hbbs, RustDesk hbbr, Glances)
   - **Total: ~34 monitors**

2. Verify in Uptime Kuma UI:
   - Go to https://status.ragnalab.xyz
   - Check monitor count matches expectation
   - Verify monitors organized by parent groups:
     - Infrastructure (HTTP)
     - Media (HTTP)
     - Apps (HTTP/TCP)
     - Containers (all container monitors)

3. Verify all monitors showing UP status:
   ```bash
   # All containers should be running
   docker ps --format "{{.Names}}: {{.Status}}" | wc -l
   ```

4. Test monitor auto-management:
   - If a service is removed/stopped, monitor should go DOWN
   - When service restarts, monitor shows UP

5. Verify autokuma tag applied to all monitors in UI.
  </action>
  <verify>
```bash
# Count all running containers
docker ps --format "{{.Names}}" | wc -l

# Check Autokuma logs for complete sync
docker logs autokuma 2>&1 | tail -20

# All containers should be running
docker compose --profile infra --profile media --profile apps ps --format "{{.Names}}: {{.State}}"
```
  </verify>
  <done>Complete monitoring coverage verified. All services across infra, media, and apps have monitors in Uptime Kuma. Monitors organized by parent groups with autokuma tag.</done>
</task>

</tasks>

<verification>
- [ ] All 9 media services have kuma labels
- [ ] All 4 app services have kuma labels
- [ ] Prowlarr, Sonarr, Radarr, Bazarr, Jellyfin, Jellyseerr have HTTP monitors
- [ ] Gluetun, qBittorrent, Unpackerr have container-only monitors
- [ ] Vaultwarden, Pi-hole, Glances have HTTP + container monitors
- [ ] RustDesk has TCP monitors for ports 21116 and 21117
- [ ] All services have container monitors
- [ ] Monitors organized into groups (Infrastructure, Media, Apps, Containers)
- [ ] All monitors have autokuma tag
- [ ] Total monitor count approximately 34
- [ ] All monitors showing UP status
</verification>

<success_criteria>
Complete Autokuma integration. All services across the entire RagnaLab stack have automatic monitoring. Uptime Kuma provides comprehensive visibility into infrastructure health. Manual monitor management eliminated.
</success_criteria>

<output>
After completion, create `.planning/phases/07-operational-hardening/07-07-SUMMARY.md`
</output>
