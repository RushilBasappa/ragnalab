---
# Ntfy

- name: Create volumes
  ansible.builtin.include_tasks: ../shared/helpers.yml
  vars:
    helper: create-volumes
    volumes: [ntfy_data]

- name: Start Ntfy
  ansible.builtin.include_tasks: ../shared/helpers.yml
  vars:
    helper: start
    services: [ntfy]
